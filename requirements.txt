# Railway (Railpack) installs from this file. Versions are PINNED, matching
# ../deckpager/requirements.txt where they overlap; loose bounds once pulled a breaking major
# into production. Phase 7 adds the analysis dependencies (anthropic, reportlab, ...) with exact pins.

fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.4
pyyaml==6.0.3
